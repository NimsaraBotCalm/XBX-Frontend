import React from 'react'


const BlueRadialGradient = ({className}) => {
  return (
    <div  className={`blue-gradient rounded-full aspect-square ${className}`}>

    </div>
  )
}

export default BlueRadialGradient